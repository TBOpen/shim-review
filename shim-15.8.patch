diff -ur shim-15.8-org/data/sbat.csv shim-15.8/data/sbat.csv
--- shim-15.8-org/data/sbat.csv	2024-01-22 11:18:05.000000000 -0800
+++ shim-15.8/data/sbat.csv	2024-02-02 17:21:34.568317428 -0800
@@ -1,2 +1,3 @@
 sbat,1,SBAT Version,sbat,1,https://github.com/rhboot/shim/blob/main/SBAT.md
 shim,4,UEFI shim,shim,1,https://github.com/rhboot/shim
+shim.terabyte,1,TeraByte,UEFI shim,2,https://www.terabyteunlimited.com
diff -ur shim-15.8-org/include/load-options.h shim-15.8/include/load-options.h
--- shim-15.8-org/include/load-options.h	2024-01-22 11:18:05.000000000 -0800
+++ shim-15.8/include/load-options.h	2024-02-02 17:14:47.972130914 -0800
@@ -13,6 +13,7 @@
 EFI_STATUS parse_load_options(EFI_LOADED_IMAGE *li);
 
 extern CHAR16 *second_stage;
+extern CHAR16 *second_stage_from_file;
 extern void *load_options;
 extern UINT32 load_options_size;
 
Only in shim-15.8/include: load-options.h.rej
diff -ur shim-15.8-org/load-options.c shim-15.8/load-options.c
--- shim-15.8-org/load-options.c	2024-01-22 11:18:05.000000000 -0800
+++ shim-15.8/load-options.c	2024-02-02 17:14:00.290936357 -0800
@@ -6,6 +6,7 @@
 #include "shim.h"
 
 CHAR16 *second_stage;
+CHAR16 *second_stage_from_file;
 void *load_options;
 UINT32 load_options_size;
 
Only in shim-15.8: load-options.c.rej
diff -ur shim-15.8-org/Make.defaults shim-15.8/Make.defaults
--- shim-15.8-org/Make.defaults	2024-01-22 11:18:05.000000000 -0800
+++ shim-15.8/Make.defaults	2024-02-02 17:14:00.290936357 -0800
@@ -28,7 +28,8 @@
 DEBUGINFO	?= $(prefix)/lib/debug/
 DEBUGSOURCE	?= $(prefix)/src/debug/
 OSLABEL		?= $(EFIDIR)
-DEFAULT_LOADER	?= \\\\grub$(ARCH_SUFFIX).efi
+DEFAULT_LOADER	?= grub$(ARCH_SUFFIX).efi
+DEFAULT_LOADER_FROM_FILE ?= shim$(ARCH_SUFFIX).dat
 DASHJ		?= -j$(shell echo $$(($$(grep -c "^model name" /proc/cpuinfo) + 1)))
 
 ARCH		?= $(shell $(CC) -dumpmachine | cut -f1 -d- | sed s,i[3456789]86,ia32,)
@@ -98,7 +99,10 @@
 endif
 
 DEFINES		= -DDEFAULT_LOADER='L"$(DEFAULT_LOADER)"' \
-		  -DDEFAULT_LOADER_CHAR='"$(DEFAULT_LOADER)"'
+		  -DDEFAULT_LOADER_CHAR='"$(DEFAULT_LOADER)"' \
+		  -DALLOW_32BIT_KERNEL_ON_X64 \
+		  -DDISABLE_FALLBACK \
+		  -DDEFAULT_LOADER_FROM_FILE='L"$(DEFAULT_LOADER_FROM_FILE)"'
 
 INCLUDES	= -nostdinc \
 		  -I$(TOPDIR)/Cryptlib -I$(TOPDIR)/Cryptlib/Include \
Only in shim-15.8: Make.defaults.rej
diff -ur shim-15.8-org/pe-relocate.c shim-15.8/pe-relocate.c
--- shim-15.8-org/pe-relocate.c	2024-01-22 11:18:05.000000000 -0800
+++ shim-15.8/pe-relocate.c	2024-02-02 17:14:00.291936382 -0800
@@ -315,7 +315,7 @@
 	return 0;
 }
 
-static const UINT16 machine_type =
+static const UINT16 UNUSED machine_type =
 #if defined(__x86_64__)
 	IMAGE_FILE_MACHINE_X64;
 #elif defined(__aarch64__)
@@ -333,15 +333,8 @@
 static int
 image_is_loadable(EFI_IMAGE_OPTIONAL_HEADER_UNION *PEHdr)
 {
-	/* If the machine type doesn't match the binary, bail, unless
-	 * we're in an allowed 64-on-32 scenario */
-	if (PEHdr->Pe32.FileHeader.Machine != machine_type) {
-		if (!(machine_type == IMAGE_FILE_MACHINE_I386 &&
-		      PEHdr->Pe32.FileHeader.Machine == IMAGE_FILE_MACHINE_X64 &&
-		      allow_64_bit())) {
-			return 0;
-		}
-	}
+	/* removed logic of 64-on-32 because it stops 32-on-64 (ALLOW_32BIT_KERNEL_ON_X64)  *
+	 * and secion not needed since allow_64_bit() already has correct logic.            */
 
 	/* If it's not a header type we recognize at all, bail */
 	switch (PEHdr->Pe32Plus.OptionalHeader.Magic) {
Only in shim-15.8: pe-relocate.c.rej
Only in shim-15.8: shim-15.8.patch
diff -ur shim-15.8-org/shim.c shim-15.8/shim.c
--- shim-15.8-org/shim.c	2024-01-22 11:18:05.000000000 -0800
+++ shim-15.8/shim.c	2024-02-02 17:20:29.857696228 -0800
@@ -780,6 +780,8 @@
 	return verify_buffer_sbat(data, datasize, context);
 }
 
+#if defined(DISABLE_REMOVABLE_LOAD_OPTIONS) || !defined(DISABLE_FALLBACK)
+
 static int
 is_removable_media_path(EFI_LOADED_IMAGE *li)
 {
@@ -813,7 +815,15 @@
 	return ret;
 }
 
+#endif
+
 static int
+#if defined(DISABLE_FALLBACK)
+should_use_fallback(EFI_HANDLE image_handle UNUSED)
+{
+	return 0;
+}
+#else
 should_use_fallback(EFI_HANDLE image_handle)
 {
 	EFI_LOADED_IMAGE *li;
@@ -869,11 +879,12 @@
 
 	return ret;
 }
+#endif
 /*
  * Open the second stage bootloader and read it into a buffer
  */
 static EFI_STATUS load_image (EFI_LOADED_IMAGE *li, void **data,
-			      int *datasize, CHAR16 *PathName)
+			      int *datasize, CHAR16 *PathName, BOOLEAN ignoremissingfile)
 {
 	EFI_STATUS efi_status;
 	EFI_HANDLE device;
@@ -906,7 +917,8 @@
 	 */
 	efi_status = root->Open(root, &grub, PathName, EFI_FILE_MODE_READ, 0);
 	if (EFI_ERROR(efi_status)) {
-		perror(L"Failed to open %s - %r\n", PathName, efi_status);
+		if (!ignoremissingfile || efi_status != EFI_NOT_FOUND)
+			perror(L"Failed to open %s - %r\n", PathName, efi_status);
 		goto error;
 	}
 
@@ -1153,7 +1165,7 @@
 		/*
 		 * Read the new executable off disk
 		 */
-		efi_status = load_image(shim_li, data, datasize, *PathName);
+		efi_status = load_image(shim_li, data, datasize, *PathName, FALSE);
 		if (EFI_ERROR(efi_status)) {
 			perror(L"Failed to load image %s: %r\n",
 			       PathName, efi_status);
@@ -1278,6 +1290,62 @@
 	return efi_status;
 }
 
+#if defined(DEFAULT_LOADER_FROM_FILE)
+/*
+ * Load file with name of image to run
+ */
+EFI_STATUS set_second_stage_from_file(EFI_LOADED_IMAGE *li)
+{
+	EFI_STATUS efi_status;
+	CHAR16 *PathName = NULL;
+	void *data = NULL;
+	int datasize;
+	EFI_DEVICE_PATH *devpath = NULL;
+
+	/*
+	 * Build a new path from the existing one plus the filename to the file with the name
+	 */
+	efi_status = generate_path(DEFAULT_LOADER_FROM_FILE, li, &devpath, &PathName);
+
+	if (efi_status != EFI_SUCCESS) {
+		perror(L"Unable to generate path %s: %r\n", DEFAULT_LOADER_FROM_FILE, efi_status);
+		return efi_status;
+	}
+	
+	/* don't need devpath */
+	FreePool(devpath);
+	devpath = NULL;
+
+	/*
+	 * Load the file if it exists
+	 */
+
+	efi_status = load_image(li, &data, &datasize, PathName, TRUE);
+
+	if (efi_status != EFI_SUCCESS) {
+		if (efi_status!=EFI_NOT_FOUND)
+			perror(L"Failed to load file %s: %r\n", PathName, efi_status);
+	}
+	/*
+	 * Check if the name appears valid
+	 */
+	else if ((datasize % sizeof(CHAR16)) == 0 &&
+            *((CHAR16*) data+(datasize/sizeof(CHAR16))-1)==0 &&
+	    StrLen((CHAR16*) data)==(datasize/sizeof(CHAR16))-1) {
+		/* Name appears valid so use it */
+		second_stage = second_stage_from_file = (CHAR16*) data;	  
+	}
+	else {
+		perror(L"Contents of %s are not supported\n", PathName);
+		efi_status=EFI_UNSUPPORTED;
+		FreePool(data);
+	}
+
+	FreePool(PathName);
+
+	return efi_status;
+}
+#endif
 /*
  * Check the load options to specify the second stage loader
  */
@@ -1297,6 +1365,12 @@
 		return efi_status;
 	}
 
+#if defined(DEFAULT_LOADER_FROM_FILE)
+	if (set_second_stage_from_file(li) == EFI_SUCCESS) {
+		return EFI_SUCCESS;
+	}
+#endif
+
 #if defined(DISABLE_REMOVABLE_LOAD_OPTIONS)
 	/*
 	 * boot services build very strange load options, and we might misparse them,
@@ -1754,6 +1828,14 @@
 	}
 
 	unhook_exit();
+	
+#if defined(DEFAULT_LOADER_FROM_FILE)
+	/*
+	 * Free the space allocated for the alternate 2nd stage loader 
+	 */
+	 if (second_stage_from_file)
+		 FreePool(second_stage_from_file);
+#endif	 
 
 	console_fini();
 }
